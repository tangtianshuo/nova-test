# 黑盒自动化测试工具 - 接口设计文档 (v1.0)

本接口设计文档基于 **OpenAPI 3.0** 规范，涵盖 AaaS (Agent as a Service) 引擎暴露给 SaaS 控制面及第三方系统调用的核心 RESTful API 以及 WebSocket 推流接口。所有接口遵循 **SDD (Schema-Driven Development)** 标准。

---

## 1. 基础约定

- **Base URL**: `https://api.aaas.example.com/v1`
- **认证方式**: 采用 Bearer Token (JWT)，请求头需包含 `Authorization: Bearer <token>`
- **数据格式**: 请求与响应默认使用 `application/json`

---

## 2. REST API 接口设计

### 2.1 创建测试任务 (Create Task)

**POST** `/tasks`

**请求体 (Request Body Schema)**:
```json
{
  "tenant_id": "string",
  "name": "电商核心购买流程测试",
  "target_url": "https://shop.example.com",
  "natural_objective": "搜索MacBook，加入购物车并结算，不进行最终付款",
  "constraints": {
    "max_steps": 50,
    "forbidden_domains": ["stripe.com", "alipay.com"]
  }
}
```

**响应体 (Response Body Schema)**:
```json
{
  "status": "success",
  "data": {
    "task_id": "task_123abc",
    "status": "CREATED",
    "created_at": "2026-03-23T10:00:00Z"
  }
}
```

### 2.2 触发 Agent 执行实例 (Trigger Agent)

**POST** `/tasks/{task_id}/instances`

*说明*：基于任务定义，启动一个全新的 Agent 沙箱实例。

**响应体**:
```json
{
  "status": "success",
  "data": {
    "instance_id": "inst_888xyz",
    "status": "PENDING"
  }
}
```

### 2.3 获取实例执行状态 (Get Instance State)

**GET** `/instances/{instance_id}/state`

*说明*：获取当前 LangGraph 的全局状态（AgentState）。

**响应体**:
```json
{
  "status": "success",
  "data": {
    "instance_id": "inst_888xyz",
    "status": "running",
    "current_step": 3,
    "current_screenshot_url": "https://s3.example.com/.../screen_3.png",
    "action_history": [
      {
        "thought": "我看到了搜索框，准备输入MacBook",
        "action_type": "type",
        "params": { "text": "MacBook" },
        "is_success": true
      }
    ]
  }
}
```

### 2.4 人机协同：接管/恢复执行 (Resume HIL)

**POST** `/instances/{instance_id}/resume`

*说明*：当 Agent 状态为 `paused_hil` 时，SaaS 前端调用此接口注入人工决策并唤醒状态机。

**请求体**:
```json
{
  "decision": "modify", 
  "modified_action": {
    "action_type": "click",
    "target": { "x": 512, "y": 256 }
  },
  "comment": "人工修正：由于遮挡，调整点击坐标"
}
```

**响应体**:
```json
{
  "status": "success",
  "message": "Agent resumed successfully"
}
```

---

## 3. WebSocket 实时流接口设计

为了实现控制台上“所见即所得”的监控体验，AaaS 引擎提供基于 WebSocket 的实时推流接口。

**Endpoint**: `wss://api.aaas.example.com/v1/ws/instances/{instance_id}/stream`

### 3.1 鉴权
建立连接时，需在 URL 参数或 Header 中携带 `?token=<jwt>`，网关验证权限后允许升级协议。

### 3.2 下发消息格式 (Server -> Client)

所有推送消息包装在标准 Schema 中：

```json
{
  "type": "event_type",
  "timestamp": 1774273000,
  "payload": {}
}
```

#### 事件类型 1: `SCREENSHOT_UPDATE`
当 Magentic-UI 沙箱捕获到新画面时触发。
```json
{
  "type": "SCREENSHOT_UPDATE",
  "payload": {
    "step": 4,
    "image_b64": "iVBORw0KGgoAAAANSUhEUgAA..." // 压缩过的Base64，降低带宽
  }
}
```

#### 事件类型 2: `AGENT_THOUGHT`
当 Fara-7B 模型推理输出 `thought` 字段时触发（可用于在UI上打字机效果展示Agent内心OS）。
```json
{
  "type": "AGENT_THOUGHT",
  "payload": {
    "thought": "正在验证购物车数字是否从0变成了1...",
    "confidence": 0.92
  }
}
```

#### 事件类型 3: `HIL_REQUEST`
当 LangGraph 遇到阻断节点时，推送请求人工介入的工单信息。
```json
{
  "type": "HIL_REQUEST",
  "payload": {
    "ticket_id": "hil_999abc",
    "reason": "检测到敏感的支付按钮，等待人工确认",
    "risk_level": "HIGH",
    "planned_action": {
      "action_type": "click",
      "target": { "x": 1024, "y": 768 }
    }
  }
}
```

---

## 4. 接口错误码设计

统一错误响应 Schema：
```json
{
  "status": "error",
  "error": {
    "code": "ERR_404",
    "message": "Agent Instance Not Found"
  }
}
```

| 错误码 | 说明 |
| --- | --- |
| `ERR_400` | 参数不符合 Schema 规范 (Validation Error) |
| `ERR_401` | Token 无效或过期 |
| `ERR_403` | 租户权限不足 |
| `ERR_404` | 找不到对应的 Task 或 Instance |
| `ERR_409` | 当前实例不处于 `paused_hil` 状态，无法 resume |
| `ERR_429` | 租户并发沙箱额度已满 (Rate Limit) |
| `ERR_500` | AaaS 引擎内部错误 (LangGraph 或 Fara-7B 异常) |